import {Controller} from "@hotwired/stimulus"
import { dispatchConsoleEvent } from "~/src/console_event_dispatcher"

// Connects to data-controller="demo"
export default class extends Controller {
  connect() {
  }
}
