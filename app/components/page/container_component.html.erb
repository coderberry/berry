<%= render UI::ContainerComponent.new do %>
  <% if render_breadcrumbs? %>
    <% if breadcrumbs? %>
      <%= render breadcrumbs %>
    <% else %>
      <%= render UI::BreadcrumbsComponent.new(page: @page) %>
    <% end %>
  <% end %>

  <%= title if title? %>

  <%= content %>
<% end %>
